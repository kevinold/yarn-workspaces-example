{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn --cwd packages/nextjs-app dev",
    "build": "yarn --cwd packages/nextjs-app build",
    "start": "yarn --cwd packages/nextjs-app start"
  }
}